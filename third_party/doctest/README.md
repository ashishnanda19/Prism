# doctest (vendored)

`doctest.h` v2.4.11 — single-header C++ test framework.
Source: https://github.com/doctest/doctest (MIT License).

Vendored so the test build is reproducible offline. To update, replace this
file with a newer tagged `doctest/doctest.h` and bump the version note above.
